= content_for :title, :title.t(:scope => [:admin, :posts, :edit])

.row
  .span12
    .widget
      .widget-content
        %h1= :h1.t(:scope => [:admin, :posts, :edit])
        = render 'menu'

= render 'form'

