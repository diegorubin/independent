= content_for :title, :title.t(:scope => [:admin, :images, :new])

.row
  .span12
    .widget
      .widget-content
        %h1= :h1.t(:scope => [:admin, :images, :new])
        = render 'menu'

= render 'form'

