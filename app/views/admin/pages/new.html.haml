= content_for :title, :title.t(:scope => [:admin, :pages, :new])

.row
  .span12
    .widget
      .widget-content
        %h1= :h1.t(:scope => [:admin, :pages, :new])
        = render 'menu'

= render 'form'

