= content_for :title, :title.t(:scope => [:admin, :pages, :edit])

.row
  .span12
    .widget
      .widget-content
        %h1= :h1.t(:scope => [:admin, :pages, :edit])
        = render 'menu'

= render 'form'

