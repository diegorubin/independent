= content_for :title, :title.t(:scope => [:admin, :presentations, :new])

.row
  .span12
    .widget
      .widget-content
        %h1= :h1.t(:scope => [:admin, :presentations, :new])
        = render 'menu'

= render 'form'

