= content_for :title, :title.t(:scope => [:admin, :presentations, :edit])

.row
  .span12
    .widget
      .widget-content
        %h1= :h1.t(:scope => [:admin, :presentations, :edit])
        = render 'menu'

= render 'form'

