= content_for :title, :title.t(:scope => [:admin, :assets, :edit])

.row
  .span12
    .widget
      .widget-content
        %h1= :h1.t(:scope => [:admin, :assets, :edit])
        = render 'menu'

= render 'form'

