= content_for :title, :title.t(:scope => [:admin, :assets, :new])

.row
  .span12
    .widget
      .widget-content
        %h1= :h1.t(:scope => [:admin, :assets, :new])
        = render 'menu'

= render 'form'

