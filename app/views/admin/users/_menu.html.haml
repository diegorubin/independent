.well
  .btn-toolbar{role: "toolbar"}
  
    - unless in_action?('new')
      .btn-group
        = menu_button(:new.t(:scope => [:admin, :users, :menu]), new_admin_user_path, 'btn-primary')
  
    - unless in_action?('index')
      .btn-group
        = menu_button(:list.t(:scope => [:admin, :users, :menu]), admin_users_path, 'btn-default')

    - if in_action?('edit')
      .btn-group
        = menu_button(:destroy.t(:scope => [:admin, :users, :menu]), admin_user_path(params[:id]), 'btn-danger', {method: 'delete', data:{confirm: :confirm.t(:scope => [:admin, :users, :destroy])}})


  
