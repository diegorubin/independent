= content_for :title, :title.t(:scope => [:admin, :snippets, :new])

.row
  .span12
    .widget
      .widget-content
        %h1= :h1.t(:scope => [:admin, :snippets, :new])
        = render 'menu'

= render 'form'

