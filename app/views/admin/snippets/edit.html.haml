= content_for :title, :title.t(:scope => [:admin, :snippets, :edit])

.row
  .span12
    .widget
      .widget-content
        %h1= :h1.t(:scope => [:admin, :snippets, :edit])
        = render 'menu'

= render 'form'

