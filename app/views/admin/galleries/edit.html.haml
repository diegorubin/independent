= content_for :title, :title.t(:scope => [:admin, :galleries, :edit])

.row
  .span12
    .widget
      .widget-content
        %h1= :h1.t(:scope => [:admin, :galleries, :edit])
        = render 'menu'

= render 'form'

